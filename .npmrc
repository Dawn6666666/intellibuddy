# 强制使用 pnpm
engine-strict=true

# pnpm workspace 配置
# node-linker=hoisted  # 改为注释，使用默认的 isolated 模式
strict-peer-dependencies=false
shamefully-hoist=false

# 自动安装 peer dependencies
auto-install-peers=true

# 性能优化
prefer-frozen-lockfile=false

